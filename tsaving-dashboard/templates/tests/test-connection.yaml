apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsaving-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "tsaving-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tsaving-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
